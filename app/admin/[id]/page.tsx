import {notFound} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {CarEditor} from '../car-editor';export default async function Edit({params}:{params:Promise<{id:string}>}){const db=await createClient();const {data}=await db.from('cars').select('*,car_images(*)').eq('id',(await params).id).single();if(!data)notFound();return <CarEditor car={data}/>}
